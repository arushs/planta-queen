{
  "name": "planta-queen",
  "version": "0.1.0",
  "description": "Plant tracking app with photo-based watering recommendations and reminders",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.client.json",
    "start": "node dist/server/index.js",
    "dev": "npm run build && npm start"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.74.0",
    "express": "^4.21.2",
    "multer": "^2.0.2",
    "sharp": "^0.34.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.23",
    "@types/multer": "^2.0.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
